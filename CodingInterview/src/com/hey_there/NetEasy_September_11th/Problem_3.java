package com.hey_there.NetEasy_September_11th;

import java.util.Arrays;

public class Problem_3 {
    public int[] split_into_list(int N, String S) {
        int[] lens = new int[N];//前N个数各自是几位数
        Arrays.fill(lens, 1);
        int totalLen = N;//记录前N个数的总长度

        return null;
    }

    public int[] split_2(String S) {
        int lenS = S.length();
        int len1 = 1, len2 = 1;
        while (len1 + len2 <= lenS / 2) {

        }
        return null;
    }
}
